import { defineConfig } from 'vite';

export default defineConfig({
  // Replace 'vamp' with your actual GitHub repo name if different.
  // e.g. if hosted at https://fionnmacanbhaird.github.io/my-game/ use '/my-game/'
  base: '/vamp/',
});
